var el = $('#green-box');


var page = el.closest('.page');

el - page = absolute height (absolute height = relative height);


el - document = fixed height (fixed height = total height);







var remaining = (scrollTop - sectionTop) / sectionHeight;
$('#green-box').css({
	position: 'absolute',
	top: 30% - (remaining / 2)
})